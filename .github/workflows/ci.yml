name: CI

on:
  push: {}

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build Sorbet
      run: ./bazel build //main:sorbet --config=release-linux

    - name: Test Sorbet
      run: ./bazel test //main:sorbet
